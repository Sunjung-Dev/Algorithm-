count = int(input())
answer = list()

for c in range(0, count):
    answer.append(int(input()))

    if c > 0:
        if answer[c-1] == answer[c]:
            pass
        elif answer[c-1] > answer[c]:
            temp = answer[c]
            answer[c] = answer[c-1]
            answer[c-1] = temp
        else:
            pass
    else: 
        pass

print(answer)

